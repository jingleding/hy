// // import _ from 'lodash'
// import actionType from '../constants/actionType'
// import assign from 'object-assign'
// const { LOAD_CHART, LOAD_TEST, LOAD_NAVS } = actionType

// let initialState = {
//     test: {},
//     navs: []
// }

// export default function (state = initialState, action) {
//     switch(action.type) {
//         case LOAD_CHART:
//             return assign({}, state, action.response)
//         case LOAD_TEST:
//             return assign({}, state, {test: action.response})//state.merge({test: action.response})
//         case LOAD_NAVS:
//             return assign({}, state, {navs: action.response})//state.merge({navs: action.response})
//         default:
//             return state
//     }
// } 
"use strict";